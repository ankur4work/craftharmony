import CartClient from '@/components/CartClient';

export const metadata = {
  title: 'Your Cart',
  description: 'Review the handcrafted pieces in your CraftHarmony cart.',
};

export default function CartPage() {
  return <CartClient />;
}
