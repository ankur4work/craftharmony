import WishlistClient from '@/components/WishlistClient';

export const metadata = {
  title: 'Wishlist',
  description: 'Your saved handcrafted pieces from CraftHarmony.',
};

export default function WishlistPage() {
  return <WishlistClient />;
}
